<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>Remli</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>

	<!-- Declaring this here means App Store Connect never stops to ask about
	     export compliance on every single upload. Remli uses only standard
	     HTTPS, which is exempt. -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>

	<!-- Shown verbatim in the system permission dialogs. Each says what Remli does
	     with the permission and, where relevant, what it does not do. -->
	<key>NSMicrophoneUsageDescription</key>
	<string>Remli listens so you can speak an idea instead of typing it. Audio is transcribed on your device and then discarded — no recording is ever saved or uploaded.</string>
	<key>NSSpeechRecognitionUsageDescription</key>
	<string>Remli turns what you say into text on your device, so a spoken idea becomes something you can search and connect later.</string>
	<key>NSCalendarsFullAccessUsageDescription</key>
	<string>Remli looks at when you're busy — never at what your events are — so it can suggest an idea that fits a gap in your day.</string>

	<key>UIBackgroundModes</key>
	<array>
		<string>fetch</string>
		<string>processing</string>
	</array>
	<!-- Must match ResurfacingCoordinator.refreshTaskIdentifier exactly, or
	     BGTaskScheduler.register throws at launch. -->
	<key>BGTaskSchedulerPermittedIdentifiers</key>
	<array>
		<string>com.chrisallenclark.remli.refresh</string>
	</array>

	<key>UILaunchScreen</key>
	<dict>
		<key>UIColorName</key>
		<string>Canvas</string>
	</dict>

	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<false/>
	</dict>

	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
	</array>

	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>arm64</string>
	</array>
</dict>
</plist>
